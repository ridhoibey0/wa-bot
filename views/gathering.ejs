<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gathering Data - WhatsApp Bot</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="dashboard">
    <!-- Sidebar -->
    <aside class="sidebar">
      <h2>🤖 WA Bot</h2>
      <nav>
        <a href="/dashboard">📊 Dashboard</a>
        <a href="/qr">📱 QR Code</a>
        <a href="/users">👥 Users</a>
        <a href="/menus">🍽️ Menus</a>
        <a href="/gathering" class="active">🎉 Gathering</a>
        <a href="/muted">🔇 Muted Users</a>
      </nav>
      <a href="/logout" class="logout">🚪 Logout</a>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
      <div class="header">
        <h1>Gathering Data</h1>
        <div class="user-info">
          👤 <strong><%= username %></strong>
        </div>
      </div>

      <!-- Gathering Table -->
      <div class="table-container">
        <h3 style="margin-bottom: 15px;">📋 Semua Pendaftaran (<%= data.length %>)</h3>
        
        <% 
          const totalPendapatan = data.reduce((sum, item) => sum + item.total, 0);
          const totalPaid = data.filter(item => item.status === 'paid').reduce((sum, item) => sum + item.total, 0);
          const totalPending = totalPendapatan - totalPaid;
        %>
        
        <div style="display: flex; gap: 20px; margin-bottom: 20px; flex-wrap: wrap;">
          <div style="background: #e3f2fd; padding: 15px; border-radius: 5px; flex: 1; min-width: 200px;">
            <strong>Total Pendapatan:</strong><br>
            <span style="font-size: 24px; color: #1976d2;">Rp <%= totalPendapatan.toLocaleString('id-ID') %></span>
          </div>
          <div style="background: #d4edda; padding: 15px; border-radius: 5px; flex: 1; min-width: 200px;">
            <strong>Sudah Bayar:</strong><br>
            <span style="font-size: 24px; color: #28a745;">Rp <%= totalPaid.toLocaleString('id-ID') %></span>
          </div>
          <div style="background: #fff3cd; padding: 15px; border-radius: 5px; flex: 1; min-width: 200px;">
            <strong>Belum Bayar:</strong><br>
            <span style="font-size: 24px; color: #856404;">Rp <%= totalPending.toLocaleString('id-ID') %></span>
          </div>
        </div>
        
        <div style="overflow-x: auto;">
          <table>
            <thead>
              <tr>
                <th>No</th>
                <th>Nama</th>
                <th>Phone</th>
                <th>Menu</th>
                <th>Harga Menu</th>
                <th>Total Bayar</th>
                <th>Status</th>
                <th>Tanggal</th>
                <th>Action</th>
              </tr>
            </thead>
            <tbody>
              <% if (data.length === 0) { %>
                <tr>
                  <td colspan="9" style="text-align: center; color: #999;">
                    Belum ada yang mendaftar
                  </td>
                </tr>
              <% } else { %>
                <% data.forEach((item, index) => { %>
                  <tr>
                    <td><%= index + 1 %></td>
                    <td><%= item.user_name %></td>
                    <td><%= item.phone %></td>
                    <td><%= item.menu_name %></td>
                    <td>Rp <%= item.price.toLocaleString('id-ID') %></td>
                    <td><strong>Rp <%= item.total.toLocaleString('id-ID') %></strong></td>
                    <td>
                      <% if (item.status === 'paid') { %>
                        <span class="badge badge-success">✓ Paid</span>
                      <% } else { %>
                        <span class="badge badge-warning">⏳ Pending</span>
                      <% } %>
                    </td>
                    <td><%= new Date(item.created_at).toLocaleDateString('id-ID') %></td>
                    <td>
                      <% if (item.status !== 'paid') { %>
                        <form action="/gathering/status/<%= item.id %>" method="POST" style="display: inline;">
                          <input type="hidden" name="status" value="paid">
                          <button type="submit" class="btn-success btn-sm">Mark Paid</button>
                        </form>
                      <% } %>
                      <form action="/gathering/delete/<%= item.id %>" method="POST" style="display: inline;" 
                            onsubmit="return confirm('Yakin ingin hapus data <%= item.user_name %>?')">
                        <button type="submit" class="btn-danger btn-sm">Delete</button>
                      </form>
                    </td>
                  </tr>
                <% }) %>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>

      <!-- Breakdown Info -->
      <div class="table-container" style="margin-top: 20px;">
        <h3 style="margin-bottom: 15px;">💡 Rincian Biaya</h3>
        <p style="color: #666;">
          Setiap peserta membayar:<br>
          • Harga Menu + PPN 10% + Biaya Sound System Rp 10.000<br>
          • Contoh: Menu Rp 50.000 → Total: Rp 50.000 + Rp 5.000 (PPN) + Rp 10.000 (Sound) = <strong>Rp 65.000</strong>
        </p>
      </div>
    </main>
  </div>
</body>
</html>
